package com.calibre

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

/**
 * The main entry point for the Calibre Android application.
 * This activity will be responsible for hosting the primary UI.
 *
 * For now, this is a placeholder to establish the project structure.
 */
class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // setContentView(R.layout.activity_main) // This will be enabled later
    }
}
